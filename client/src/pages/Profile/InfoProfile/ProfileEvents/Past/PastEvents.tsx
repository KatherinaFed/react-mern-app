
import ItemCard from '../../../../../components/ItemCard/ItemCard';
import { PastEventsContainer } from './styles';

const PastEvents = () => {
  return (
    <PastEventsContainer>
      {/* {pastList.map(({ id, activity, date, time, location, creator, img, status }) => (
        <ItemCard
          id={id}
          activity={activity}
          date={date}
          time={time}
          location={location}
          creator={creator}
          img={img}
          status={status}
        />
      ))} */}
    </PastEventsContainer>
  );
};

export default PastEvents;
